import { Logo } from '@components/icons';
import React from 'react';

const Loading = () => {
    return (
        <div className='d-flex m-auto'>
            <Logo /> | <p>just in time.</p>
        </div>
    );
};

export default Loading;
